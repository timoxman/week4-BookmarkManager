  # can't move this code here!

  # helpers do
  #   #let's create a helper that will give us access to the current user, if logged in - part of sinatra
  #   def current_user
  #     @current_user ||= User.get(session[:user_id]) if session[:user_id]
  #   end

  # end